{
  "name": "logos",
  "version": "1.0.0",
  "description": "Convert and optimize logos",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "postversion": "git push && git push --tags",
    "svg": "ts-node src/svg2png.ts",
    "fav": "ts-node src/fav.ts",
    "clean": "shx rm -rf dist/*",
    "build": "ts-node ./src/logo-fav.ts",
    "build-test": "ts-node ./src/ftc.ts"
  },
  "author": "weiguo.ni",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.11.6",
    "shx": "^0.3.4",
    "ts-node": "^10.9.2",
    "tslib": "^2.6.2",
    "typescript": "^5.2.2"
  },
  "dependencies": {
    "favicons": "^7.1.5",
    "sharp": "^0.33.2",
    "svgo": "^3.2.0"
  }
}
